spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edumantra?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Rania@01
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # Use "create-drop" only for development, "update" for existing data
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
